<%= form_for(wynthought) do |f| %>
  <% if wynthought.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(wynthought.errors.count, "error") %> prohibited this wynthought from being saved:</h2>

      <ul>
      <% wynthought.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <!-- <div class="field">
    <%= f.label :title %>
    <%= f.text_field :title %>
  </div> -->

  <div class="field">
    <%= f.label :body %>
    <%= f.text_area :body %>
  </div>

  <div class="field">
    <!-- <%= f.label :suggestion_id %> -->
    <%= f.hidden_field :suggestion_id, value: @suggestion.id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
